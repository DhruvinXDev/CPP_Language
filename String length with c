#include<stdio.h>
void main ( )
{
    static int len;
    char s[50];
    printf("Enter the string :");
    gets(s);
    for(int i=0;s[i]!='\0';i++)
    {
        len++;
    }
    printf("Your string length is %d.",len);
}
